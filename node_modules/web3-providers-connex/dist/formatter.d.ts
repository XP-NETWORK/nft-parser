/// <reference types="@vechain/connex-types" />
/// <reference types="@vechain/connex-framework/dist/driver-interface" />
import { JsonRpcPayload, RetLog, RetReceipt, RetBlock, RetTransaction, RetHeader } from './types';
export declare class Formatter {
    readonly _connex: Connex;
    private readonly _inputFormatters;
    private readonly _ifSetNet;
    constructor(connex: Connex, ifSetNet: boolean);
    formatInput: (payload: JsonRpcPayload) => any[] | Error;
    private _getBlockByNumber;
    private _getBalance;
    private _getCode;
    private _getStorageAt;
    private _sendTransaction;
    private _call;
    private _estimateGas;
    private _getLogs;
    private _subscribe;
    private _sendRawTransaction;
    outputReceiptFormatter: (receipt: Connex.Thor.Transaction.Receipt) => RetReceipt;
    outputBlockFormatter: (b: Connex.Thor.Block) => RetBlock;
    outputTransactionFormatter(tx: Connex.Thor.Transaction): RetTransaction;
    outputLogsFormatter(ret: Connex.Thor.Filter.Row<'event'>[]): RetLog[];
    outputHeaderFormatter: (b: Connex.Thor.Block) => RetHeader;
}
